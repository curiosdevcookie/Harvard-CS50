minuscule = "cat"
capitalized = minuscule.capitalize()
maiuscule = capitalized.upper()

print(f"minuscule: {minuscule} | capitalized: {capitalized} | maiuscule: {maiuscule} ") 
# because we change a copy here as strings are immutable...